# Meloscape 项目开发复盘

> 项目：Meloscape — 实时音乐可视化 Web 应用  
> 复盘时间：2026-07-07  
> 主要角色：产品 + 前端开发（AI 搭档协作）  
> 代码仓库：https://github.com/Maropion03/Meloscape  
> 线上地址：https://meloscape-inn2.vercel.app

---

## 一、项目概述

Meloscape 是一个基于 Web Audio API 的实时音乐可视化应用。用户粘贴网易云音乐 / QQ 音乐 / 酷狗链接或上传本地音频后，应用会根据节奏（BPM）、情绪能量和歌曲结构自动切换 5 组像素艺术风格的动态场景。

本次迭代的核心目标：
1. 让场景切换更准、更稳，减少误判和频繁跳变
2. 优化前端信息架构与交互细节（歌词、播放控制、进度条）
3. 完成 Vercel 线上部署，并接入个人作品集网站

---

## 二、关键时间线与改动概览

| 阶段 | 时间 | 关键动作 |
|------|------|---------|
| 阈值与情绪监控 | 会话前期 | 放宽情绪阈值，加入歌词情绪监控与颜色可视化 |
| 多曲风测试 | 会话中期 | 用不同 BPM、不同曲风的歌曲验证场景切换 |
| 问题修复 | 会话中期 | 修复 `bpmAdjusted` 声明顺序、playback position 继承 bug、进度条不同步 |
| 前端打磨 | 会话中后期 | favicon、标题间距、播放/暂停切换按钮、歌词模块放大与滚动、情绪数值自适应 |
| 部署与文档 | 会话后期 | Vercel 部署、README 中英文版本、宣传图、即刻体验链接 |
| 作品集接入 | 会话末期 | 将项目加入个人网站，补充 Eval Studio 在线体验链接 |

---

## 三、目标达成情况

| # | 目标项 | 目标值 | 实际值 | 达成状态 |
|---|--------|-------|-------|---------|
| 1 | 场景切换准确率（主观听感） | 流行曲副歌才到 energetic，中低能量曲不频繁跳变 | 中低能量曲稳定，副歌能触发高能量场景 | ✅ 达成 |
| 2 | 前端交互完成度 | 完成 favicon、播放按钮、歌词布局、进度条同步 | 全部完成 | ✅ 达成 |
| 3 | 线上可访问性 | Vercel 部署后可公开访问 | 已部署至 `meloscape-inn2.vercel.app` | ✅ 达成 |
| 4 | 文档与传播 | README 含宣传图、即刻体验、中英双语 | 已完成 | ✅ 达成 |
| 5 | 接入个人作品集 | 在个人网站展示项目卡片 | 已接入 `maropion.fun` | ✅ 达成 |

### 总体评价
- 达成率：5/5
- 一句话总结：从阈值调优到部署落地的完整闭环完成，过程中暴露的部署认知和域名配置问题都已解决。

---

## 四、关键问题与归因

### 问题 1：流行曲前奏刚过就被判定为 energetic

**现象**：测试流行曲时，刚过前奏就进入 energetic / sunset 场景，与预期"只有副歌才到 energetic"不符。

**5-Why 归因**：
- Why 1：情绪分数计算结果偏高
- Why 2：upbeat 阈值 0.48、energetic 阈值 0.70 对流行曲整体响度偏大的音乐过低
- Why 3：能量计算基于全曲相对能量，流行曲动态范围小，导致多数段落被高估
- Why 4：build nudge（副歌预测加成）对副歌位置判断不稳定，容易提前触发
- Why 5：阈值设计时缺少多曲风样本校准，默认参数偏向电子/重节奏音乐

**根因**：阈值和能量归一化策略未针对流行曲校准
**根因类型**：设计问题（可控）
**责任方**：算法/参数设计

**修复动作**：
- 将 melancholic 阈值调整为 0.12–0.40，upbeat 调整为 0.32–0.52
- 引入 2.5s 滑动基线（relative energy）替代全曲相对能量
- 重置每首新歌的 lyric emotion 历史，避免跨歌曲污染

---

### 问题 2：切换新歌时从旧歌播放位置继续播放

**现象**：上一首歌在 1:30 暂停，切换新歌后从新歌的 1:30 开始播放。

**根因**：`pauseOffset` 未在加载新文件/链接时重置，AudioBufferSourceNode 的 start 时间继承了旧状态。
**根因类型**：代码缺陷（可控）
**责任方**：前端开发

**修复动作**：在 `loadFile()` 和 `loadUrl()` 中重置 `pauseOffset = 0`，并调用 `resetAnalysis()`。

---

### 问题 3：进度条与 track progress 不同步

**现象**：底部进度条和实际播放进度不一致。

**根因**：进度条使用了独立的计算变量，没有和统一进度变量 `songProgress` 绑定。
**根因类型**：代码缺陷（可控）
**责任方**：前端开发

**修复动作**：将 visualizer playhead 和 track progress 都绑定到同一个 `songProgress` 变量。

---

### 问题 4：Vercel 部署后访问 Not Found

**现象**：自行部署到 `meloscape` 项目后访问报 Not Found / 需要登录。

**根因**：
1. Vercel 自动识别 `server.js` 为服务入口，静态资源未被打包进函数
2. 新建项目 `meloscape` 未正确识别入口
3. Vercel Authentication（Deployment Protection）默认开启，导致外部访问需要登录
4. 域名 `maropion.fun` 实际指向 GitHub Pages，而非 Vercel

**根因类型**：部署配置 + 域名配置（可控）
**责任方**：部署/运维

**修复动作**：
- `vercel.json` 添加 `"framework": null`，强制按静态网站 + `api/` 函数模式部署
- 关闭 Deployment Protection
- 将背景图和 favicon 提交到仓库（不再依赖被 ignore 的 `assets/`）
- 明确个人网站 `maropion.fun` 走 GitHub Pages，需同步更新 `index.html`（GitHub Pages 默认入口）

---

### 问题 5：个人网站更新后未生效

**现象**：push 后 `maropion.fun` 仍显示旧版，没有 Meloscape 卡片。

**根因**：
- 修改的是 `index v4.html`（Vercel 通过 `vercel.json` 重定向使用）
- GitHub Pages 默认入口是 `index.html`，未同步更新

**根因类型**：部署配置理解偏差（可控）
**责任方**：前端开发

**修复动作**：将 `index v4.html` 同步复制到 `index.html`，确保 GitHub Pages 和 Vercel 都服务最新版本。

---

## 五、经验沉淀

### ✅ 做对了什么（继续保持）

| # | 做对的事 | 具体表现 | 建议固化 |
|---|---------|---------|---------|
| 1 | 用真实歌曲做多轮测试 | 通过《城》和多首不同 BPM 歌曲验证阈值，而不是只看代码 | 后续任何音频/视觉项目都保留"测试歌单" |
| 2 | 情绪阈值用 hysteresis（进入/退出双阈值） | 避免场景在两个状态之间频繁抖动 | 作为实时情绪分类的默认设计模式 |
| 3 | 歌词情绪可视化做得轻量 | 用颜色条和数值百分比展示，不阻塞歌词阅读 | 后续数据可视化优先"可读性" |
| 4 | 部署时分清静态资源与 API 函数 | 最终用 `"framework": null` + `api/` 目录实现稳定部署 | 写入 Vercel 部署 checklist |
| 5 | 文档同步更新（README、宣传图、中英双语） | 项目可立即对外展示和传播 | 每次上线都把 README 更新作为交付项 |

### ❌ 做错了什么（立即停止）

| # | 做错的事 | 造成的后果 | 根因 |
|---|---------|----------|------|
| 1 | 前期阈值设计缺少多曲风样本 | 流行曲误判率高，反复调参 | 设计阶段未做充分测试 |
| 2 | 把 `assets/` 加入 `.gitignore` 又依赖它部署 | Vercel 上图片缺失，页面空白 | 没区分"用户私有资源"和"项目必需资源" |
| 3 | 修改 `index v4.html` 时忘记同步 `index.html` | GitHub Pages 显示旧版 | 多入口文件未统一管理 |
| 4 | 用 Vercel 控制台链接当"在线体验" | 访问者会被要求登录 | 混淆了 dashboard URL 和 public deployment URL |

### 💡 方法论沉淀

| # | 经验原则 | 适用场景 | 具体做法 |
|---|---------|---------|---------|
| 1 | 音频可视化先做"稳"再做"炫" | 任何实时音频/视觉项目 | 先确保场景切换稳定、无 bug，再优化视觉效果 |
| 2 | 阈值参数必须跨样本校准 | 涉及分类/分层的算法 | 至少准备 3–5 种典型样本（流行、电子、古典、摇滚、舒缓） |
| 3 | 部署前明确入口文件和托管平台 | 任何静态/混合部署 | 列出：默认入口文件、托管平台规则、自定义域名指向 |
| 4 | 静态资源要么内联、要么提交仓库 | 任何需要线上展示的项目 | 不依赖 `.gitignore` 目录做线上部署 |

---

## 六、后续行动项

| # | 行动项 | 关联问题 | Owner | 截止日期 | 完成标准 |
|---|--------|---------|-------|---------|---------|
| 1 | 准备一组标准测试歌曲（流行/电子/古典/摇滚/舒缓） | 阈值校准 | 产品/开发 | 下次迭代前 | 5 首歌曲 + 期望场景标签 |
| 2 | 将 Vercel 部署配置整理成 checklist | 部署 Not Found | 开发 | 2026-07-14 | 文档包含 `vercel.json`、入口、保护开关、域名指向 |
| 3 | 统一个人网站入口文件管理 | GitHub Pages 旧版 | 开发 | 2026-07-14 | 确定唯一源文件，构建时自动同步 |
| 4 | 评估把背景图改为 Base64 内联的可行性 | assets 依赖 | 开发 | 2026-07-21 | 文件体积 < 2MB 时 acceptable |
| 5 | 收集线上真实用户反馈 | 准确率验证 | 产品 | 持续 | 至少 3 条外部用户反馈 |

---

## 七、待确认项

- [ ] `maropion.fun` 域名是否计划长期指向 GitHub Pages，还是未来切到 Vercel？
- [ ] 是否需要为 Meloscape 设置一个更稳定的 Vercel 生产域名（非随机子域名）？
- [ ] 后续是否增加更多曲风测试和用户可手动调节阈值的调试模式？
